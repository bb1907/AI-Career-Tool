import 'package:flutter/widgets.dart';

import 'resume_input_page.dart';

class ResumePage extends StatelessWidget {
  const ResumePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const ResumeInputPage();
  }
}
